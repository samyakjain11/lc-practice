#include <iostream>
#include <vector>
#include <string>
#include <cctype>
using namespace std;

class Solution {

public:
    vector<string> fetchNextAddresses(string s, vector<string> constructed) {
        
    }

    vector<string> restoreIpAddresses(string s) {
        // four integers between 0-255
        // backtracking

    }
};